# 採用シミュレーション 改善版(v2) 変更点まとめ

- **旧実装**：`src/App.jsx`（React + Vite + recharts、5ターンのLINE型チャット、`api/chat.js`はGeminiプロキシだが不整合）
- **新実装**：`recruit-sim-v2.html`（単一HTML・ビルド不要・オフライン動作、3ステージ制の面談ビュー、Gemini連携）＋ `api/chat.js` 修正

---

## 0. フィードバック対応表

| # | フィードバック | 対応 |
|---|---|---|
| 1 | 3ステージ構成／各3ターンほど | 5フラットターン → **3ステージ×3ターン(=9)** のネスト構造に再設計 |
| 2 | 攻略対象の情報をいつでも確認 | プレイ中ヘッダーに「📋 学生情報」→ **常時開けるスライドドロワー**を追加 |
| 3 | 相手のリアクションが明確に | **表情パネル＋ト書き＋定性バッジ(刺さった/手応え/響かず/地雷)** を毎ターン明示 |
| 4 | 中間報告を各ステージ終了時に | **中間報告画面**を各ステージ後に挿入（温度推移・よかった点・もったいない点・心境の読み） |
| 5/7 | 面談感がない／チャットUIすぎ | LINE風吹き出し・オンライン表示を廃し、**面談ルーム（表情パネル＋場面情景＋面談録＋発言入力）**に再構成 |

---

## 1. 構成・データモデルの変更

| 項目 | 旧 | 新 |
|---|---|---|
| 進行単位 | `SCENARIOS`（5件フラット） | `STAGES`（3ステージ×`turns`3件） |
| ターン管理 | `turn`(0–4) | `stageIdx` / `turnIdx` と `globalTurn()`(0–8) |
| 感情段階の定数名 | `STAGES`（😨〜😍） | **`MOODS` にリネーム**（ステージ構造と名前衝突を解消） |
| 承諾ライン | `THRESHOLD=75` | 同じ（75） |
| 初期スコア | 48 | 48（`stageStartScore`を追加し中間報告で使用） |

**ステージ定義（新規）**
- Stage1「出会い・信頼づくり」：説明会後の初回接触／就活軸のヒアリング／一次面接への動機づけ
- Stage2「深耕・動機づけ」：一次面接後フォロー／社員紹介・見学／「他社から内定」の一報
- Stage3「意思決定・クロージング」：他社比較への向き合い／最終面接の案内／内定承諾クロージング

---

## 2. 画面の変更

| 画面 | 旧 | 新 |
|---|---|---|
| opening | あり | あり（コピーを「面談」基調に。🤝） |
| briefing（作戦会議） | あり | あり＋「面談中も学生情報を確認できる」旨を明記 |
| play | LINE型チャット | **面談ルーム**に全面刷新（後述） |
| **stageReport（中間報告）** | なし | **新規**：各ステージ終了時に挿入 |
| result | recharts折れ線＋ターン別FB | **自作SVG折れ線**＋**ステージ別**FBに再編 |
| ranking | あり | あり（`MOODS`表示に追随） |

---

## 3. 面談ビュー化（#5/#7）

- **表情パネル（新規）**：上部に学生の大きな顔（温度で 😨→🧑‍🎓→😊 と変化）＋名前＋温度感バー。「同席している」感を演出。
- **場面情景ヘッダー（新規）**：ターンごとに「📍 一次面接後、少し不安そう…」等のシーン説明。
- **面談録スタイルのログ**：緑のLINE吹き出しを廃止。話者ラベル（`佐藤` / `あなた（人事）`）付きの紙面風カードに変更。
- **LINE的要素の除去**：`オンライン` 点滅表示・既読風演出・送信機の飛行アニメを削除。
- 入力欄を「あなたの発言（面談中）」、アクションを面談中の行動として再フレーミング。

---

## 4. リアクションの明確化（#3）

- **ト書き（新規）**：返答直後に「（目を見て、少し前のめりになった）」等の仕草描写を表情パネル下に表示。
- **定性リアクションバッジ（新規）**：`sasaru(刺さった)/good(手応え)/flat/miss(響かず)/jirai(地雷)` を色付きで明示。数値スコアは非公開のまま「手応え」だけ可視化。
- 温度の上下方向（▲▼）もバッジに併記。
- 旧実装で結果まで隠していた「理由・本音」は、**理由はプレイ中に手応えとして示し**、本音は従来通り結果で開示。

---

## 5. 中間報告（#4・新規）

各ステージ最終ターン後に `stageReport` を表示：
- ステージ内の**温度推移**（開始→終了のMOOD絵文字と上昇/低下）
- **よかった対応**（そのステージで最も delta が高かった発言＋理由）
- **もったいなかった点**（最も低かった発言＋理由）
- **学生の今の心境（読み）**＋地雷を踏んだ場合の警告
- 「次のステージへ ▶」／最終ステージ後は「結果を見る 🎉」

---

## 6. 学生情報の常時確認（#2・新規）

- プレイ画面ヘッダーに「📋 学生情報」ボタン → 右からスライドインするドロワー。
- 公開プロフィール／自社／競合をいつでも参照可能（本音・地雷は非公開のまま）。

---

## 7. AIバックエンド（Gemini連携）

| 項目 | 旧 | 新 |
|---|---|---|
| フロント送信モデル | `claude-sonnet-5`（プロキシはGeminiで**不整合→常時フォールバック疑い**） | `gemini-2.5-flash`（`AI.model`で変更可、最新は`gemini-3.5-flash`） |
| プロキシ既定モデル | `gemini-2.0-flash`（**2026/6/1提供終了**） | `gemini-2.5-flash`＋非Geminiモデル名を安全に矯正 |
| 出力の受け取り | テキストから`{ }`を手動抽出（**脆い**） | **構造化出力（responseSchema）**で有効JSONを強制＋`parseLoose()`で二重に堅牢化 |
| thinking | 未設定（JSONが空になる恐れ） | `thinkingConfig.thinkingBudget=0` で無効化（高速・空出力回避） |
| 呼び出し経路 | `/api/chat`のみ | `AI.geminiKey`があれば**直接Gemini**（file://でも可）／無ければ`/api/chat`プロキシ |
| 失敗時 | 定型フォールバック文 | **内蔵ルールエンジンでオフライン継続**＋画面に「⚪ オフライン体験モード」表示 |
| 応答形式 | reply/delta/reason/inner/ng | ＋ **kind（表情分類）／togaki（ト書き）** を追加 |

**プロキシ(`api/chat.js`)の修正点**
- 既定モデルを現行版へ更新、`claude-*`等が来ても`gemini-2.5-flash`に矯正
- `json:true` で `responseMimeType:'application/json'`、`response_schema` を受理
- `thinkingConfig.thinkingBudget=0` を追加

> 実測：`gemini-2.5-flash`＋responseSchema で有効JSONを安定生成（reply/delta/kind/togaki等）。一時的な高負荷エラー時はオフラインエンジンへ自動切替を確認済み。

---

## 8. 実装・技術スタックの差分

| 項目 | 旧 | 新 |
|---|---|---|
| 形態 | React + Vite（要ビルド） | **単一HTML＋vanilla JS（ビルド不要）** |
| 依存 | react / react-dom / recharts / Tailwind CDN | **外部依存なし**（CSS/JS全てインライン） |
| グラフ | recharts `LineChart` | **自作インラインSVG**の折れ線チャート |
| JSONパース | `indexOf('{')..lastIndexOf('}')` | responseSchema＋`parseLoose()`（フェンス除去・二重引用符補修） |
| オフライン | 不可（CDN必須） | **可**（そのまま開けば内蔵エンジンで動作） |

---

## 9. 実行方法（3通り）

1. **手軽（推奨・オフラインでも可）**：`recruit-sim-v2.html` の先頭 `AI.geminiKey` にAPIキーを貼り付けて保存 → ファイルをブラウザで開くだけ。学生応答は直接Geminiが生成。
   - ※キーがHTMLに残るため、共有ファイルには入れない（イベント端末ローカル用途を想定）。
2. **プロキシ経由（キーをHTMLに置かない）**：`.env`に`GEMINI_API_KEY`、`api/chat.js`を配信できる環境（`vercel dev`／本番Vercel等）で、HTMLを同一オリジンから配信。`AI.geminiKey`は空のまま。
3. **キー無し**：何も設定しなければ内蔵ルールエンジンで動作（デモ・UI確認用）。

---

## 10. 既知の注意点・今後

- `AI.geminiKey`直書きはクライアントに露出する。恒久運用はプロキシ経由（方式2）を推奨。
- `gemini-3.5-flash` に上げると品質↑・費用↑（1,000プレイ ~$14→~$57。いずれも規模的に軽微）。
- 既存の `src/App.jsx`（旧React版）は据え置き。必要なら同ロジックをReactへ移植可能。
- タイマーは180秒/ターンを踏襲（9ターン化に伴い運用に応じ調整余地あり）。
